<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
    <div>
        <ul>
            <li>
                <a href="{{ path('list_invoice') }}">Facture</a>
            </li>
            <li>
                <a href="{{ path('list_product') }}">Produit</a>
            </li>
        </ul>
    </div>
        {% block body %}{% endblock %}
    </body>
</html>
